The project aims at training a model to classify handwritten digits from the  the Keras MNIST dataset. 

The model uses the tensorflow deep learning framework (Artificial Neural Network) for training and classification.